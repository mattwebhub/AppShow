# Test plan: name

Written before any production code. Layer names come from `planning/tdd-strategy.md`.

| Spec # | Test name | Layer | File | Fixture | Status |
|--------|-----------|-------|------|---------|--------|
| 1 | `zoomKeyframes_interpolateHermite_betweenTwoPoints` | unit | `ReframedTests/Editor/ZoomTests.swift` | none | red |

## Manual checks

Only for behavior that cannot be automated (permissions, hardware). Keep the list short and say why each item cannot be a test.
