# Tasks: name

Ordered red-green-refactor steps. One commit per step is the default.

- [ ] 1. Red: write `testName`, run `make test`, confirm it fails for the right reason.
- [ ] 2. Green: minimal change in `Reframed/...`.
- [ ] 3. Refactor: ..., tests stay green, `make format`.
